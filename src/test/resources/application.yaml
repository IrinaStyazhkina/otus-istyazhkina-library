spring:
  datasource:
    url: jdbc:h2:mem:testdb
    initialization-mode: always
    data: test_data.sql
    schema: schema.sql

  h2:
    console:
      path: /h2-console
      settings:
        web-allow-others: true

  shell:
    interactive:
      enabled: false